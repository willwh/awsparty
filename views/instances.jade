h1= title
p Here are your #{title}
div(id="Instances")

p This is # { instances . requestId }

#{ instances.requestId }

p JSON.stringify the response

instances= whole

p Each val, key in response
each val, key in instances
  li #{key}: #{val}
